<h1>Create a new Genre</h1>

<form action="/genres" method="POST">
  <label>Name:</label>

  <br>

  <input type="text" name="genre[name]"id="genre_name">

  <br>
  <p>Select an existing movie or create a new one below.</p>

  <h3>Existing movies</h3>
  <% @movies.each do |movie| %>
    <input type="checkbox" name="genre[movie_ids][]" id="<%= movie.id %>" value="<%= movie.id %>"><%= movie.name %></input><br>
  <% end %>

    <br>
    <h3>New Movie</h3>
    <label>Movie Name: </label>
    <br>
    <input type="text" name="movie[name]" id="movie_name"></input>
    <br><br>